{
  "name": "pt-surya-inti-gas",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd Backend && php artisan serve --host=0.0.0.0 --port=8000\" \"cd Frontend && npm run dev\" --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\"",
    "start:backend": "cd Backend && php artisan serve --host=0.0.0.0 --port=8000",
    "start:frontend": "cd Frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
